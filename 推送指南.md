# 推送 Neovim 配置到 GitHub - 快速指南

## 🎯 当前状态

你已经有了：
- ✅ 完整的 Neovim 配置（在当前分支）
- ✅ GitHub 仓库: https://github.com/ykkaixin/vimrc
- ✅ 自动化推送脚本: `push_config.sh`

## 🚀 一键推送（推荐）

在当前目录运行：

```bash
./push_config.sh
```

脚本会引导你完成：

1. **选择仓库名称**
   - nvim-config（推荐）
   - modern-nvim
   - nvim-ide
   - neovim-setup
   - nvim-python-ide
   - vimrc（保持原名）
   - 自定义名称

2. **重命名仓库（如果需要）**
   - 如果选择了新名称，脚本会提示你在 GitHub 上重命名仓库
   - 或者创建新仓库

3. **自动推送**
   - 自动更新 README 中的仓库引用
   - 创建干净的 Git 历史
   - 推送到 GitHub

## 📝 方案选择

### 方案 A：重命名现有的 vimrc 仓库（推荐）

**优点：** 保留仓库历史，URL 会自动重定向

**步骤：**

1. 访问 https://github.com/ykkaixin/vimrc/settings
2. 找到 "Repository name"
3. 改为新名称（如 `nvim-config`）
4. 点击 "Rename"
5. 运行 `./push_config.sh` 并选择相同的新名称

### 方案 B：创建全新仓库

**优点：** 全新开始，没有旧数据

**步骤：**

1. 访问 https://github.com/new
2. Repository name: 输入新名称（如 `nvim-config`）
3. **不要勾选**任何初始化选项（README、.gitignore、license）
4. 点击 "Create repository"
5. 运行 `./push_config.sh` 并选择相同的新名称

### 方案 C：保持 vimrc 名称

**优点：** 最简单，无需任何额外操作

**步骤：**

1. 运行 `./push_config.sh`
2. 选择选项 6（vimrc）
3. 完成

## 🎨 推荐的仓库名称

| 名称 | 特点 | 适合场景 |
|------|------|----------|
| **nvim-config** ⭐ | 简洁专业 | 通用配置，推荐首选 |
| **modern-nvim** | 强调现代化 | 突出新特性和工具 |
| **nvim-ide** | 强调IDE功能 | Python/多语言开发 |
| **neovim-setup** | 完整配置 | 详细的完整设置 |
| **nvim-python-ide** | Python专用 | Python开发环境 |
| **vimrc** | 经典命名 | 传统Vim用户 |

## ⚡ 快速命令总结

**最简单的方式（3步）：**

```bash
# 1. 确保在正确的分支
git checkout claude/convert-to-neovim-config-011CUpE8FRmAhTtpmh5TFZv3

# 2. 运行推送脚本
./push_config.sh

# 3. 根据提示选择仓库名称
```

完成！🎉

## 💻 推送成功后

在任何新电脑上使用：

```bash
# 克隆仓库（替换 REPO_NAME 为你的仓库名）
git clone https://github.com/ykkaixin/REPO_NAME.git
cd REPO_NAME

# 运行安装脚本
./install.sh

# 启动 Neovim
nvim
```

首次启动时插件会自动安装（2-5分钟）。

## 🤖 配置 AI 补全

在 Neovim 中运行：

```vim
:Codeium Auth
```

按提示在浏览器中认证即可。

## ❓ 常见问题

### Q: 推送失败怎么办？

**A:** 检查：
1. 仓库名称是否正确
2. 仓库是否已创建
3. 网络连接是否正常

### Q: 想修改仓库名称怎么办？

**A:** 可以随时在 GitHub 仓库设置中重命名，Git 会自动重定向。

### Q: 可以删除旧的 XVim2 目录吗？

**A:** 推送成功后可以安全删除：
```bash
rm -rf ~/XVim2
```

---

**现在就运行 `./push_config.sh` 开始吧！** 🚀
